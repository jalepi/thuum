import { createChannel as createMessageChannel } from "./message-channel";
import { createChannel as createRequestChannel } from "./request-channel";

export { createMessageChannel, createRequestChannel };
